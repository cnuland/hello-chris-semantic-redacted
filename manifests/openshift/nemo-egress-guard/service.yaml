apiVersion: v1
kind: Service
metadata:
  name: nemo-egress-guard
  namespace: semantic-redacted
  labels:
    app: nemo-egress-guard
    app.kubernetes.io/name: nemo-egress-guard
    app.kubernetes.io/component: egress-guard
    app.kubernetes.io/part-of: semantic-redacted
spec:
  selector:
    app: nemo-egress-guard
  ports:
    - port: 8003
      targetPort: 8003
      protocol: TCP
      name: http
  type: ClusterIP
