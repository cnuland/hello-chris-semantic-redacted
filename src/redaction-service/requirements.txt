fastapi>=0.110.0
uvicorn>=0.29.0
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
spacy>=3.7.0
pyyaml>=6.0
pydantic>=2.0.0

# Download the spaCy model after install:
#   python -m spacy download en_core_web_sm

# GLiNER is optional -- the service degrades gracefully if not installed.
# Uncomment the line below to enable zero-shot entity detection:
# gliner>=0.2.0
